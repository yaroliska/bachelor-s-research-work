export default class Arrow {

  constructor(arrowId,x,y,arrowType,listOfPaths){
    this.arrowId = arrowId;
    this.x=x;
    this.y=y;
    this.arrowType= arrowType;
    this.listOfPaths =listOfPaths;
  }
}
